/** Crew home base. Shown as read-only in Settings ("customizable later") - kept as one
 * constant so the calendar's outbound/return direction and DaySheet's return-flight
 * suggestion can't drift apart. */
export const HOME_BASE_IATA = "DXB";
