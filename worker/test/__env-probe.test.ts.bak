import { describe, it, expect } from "vitest";
describe("env probe", () => {
  it("shows LIVE_PROVIDER_TEST", () => {
    console.log("PROBE:", JSON.stringify(process.env.LIVE_PROVIDER_TEST));
    expect(true).toBe(true);
  });
});
