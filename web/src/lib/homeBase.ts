/** Crew home base. Shown as read-only in Settings ("customizable later") - kept as one
 * constant so the calendar's outbound/return direction glyphs (TripsCalendar) always agree
 * with whatever "home" means elsewhere in the app. */
export const HOME_BASE_IATA = "DXB";
